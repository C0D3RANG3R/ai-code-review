{
  "name": "full-stack-ai-reviewer",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build:frontend": "cd frontend && npm install && npm run build",
    "postinstall": "echo 'Skipping unnecessary postinstall in root.'"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18.0.0" 
  }
}
