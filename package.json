{
  "name": "full-stack-ai-reviewer",
  "version": "1.0.0",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "deploy:setup": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "build": "npm run deploy:setup"
  },
  "dependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18.0.0" 
  }
}
